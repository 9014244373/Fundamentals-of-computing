#include <stdio.h>

int main() {
    int n, sum = 0;
    
    printf("Enter the range: ");
    scanf("%d", &n);
    
    printf("Odd numbers from 1 to %d:\n", n);
    
    for (int i = 1; i <= n; i += 2) {
        printf("%d ", i);
        sum += i;
    }
    
    printf("\nThe sum of odd numbers from 1 to %d is: %d", n, sum);
    
    return 0;
}
