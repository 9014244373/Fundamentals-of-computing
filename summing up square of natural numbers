#include <stdio.h>

int main() {
    int n, sum = 0;
    
    printf("Enter the range: ");
    scanf("%d", &n);
    
    printf("Sum of squares of natural numbers up to %d:\n", n);
    
    for (int i = 1; i <= n; i++) {
        int square = i * i;
        printf("%d^2 = %d\n", i, square);
        sum += square;
    }
    
    printf("The sum of squares of natural numbers up to %d is: %d", n, sum);
    
    return 0;
}
